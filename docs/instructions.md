Do not attempt to install any packages.
To execute code locally, use a singularity wrapper defined as `wrapper.sh python ...`.
To execute code on the GPU using slurm, use `sbatch wrapper.sh python ...`. 
Do not use emojis.
Do not create lengthy summary documents.
